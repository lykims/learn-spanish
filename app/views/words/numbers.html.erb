<% provide(:title, "Numbers") %>
<div class="page-header">
    <h1>
        Numbers
        <% if logged_in? %>
            <div class="pull-right">
                <%= link_to "Start Test", test_path("Numbers"), class: "btn btn-primary" %>
            </div>
        <% end %>
    </h1>
</div>

<div class="col-xs-6 col-xs-offset-3 col-md-4 col-md-offset-4">
    <%= render 'words/words_table', :en_col_size => '4', :es_col_size => '8', :words => @words %>
</div>
