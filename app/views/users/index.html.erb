<% provide(:title, "Learners") %>
<div class="page-header">
    <h1>Learners</h1>
</div>

<%= render 'shared/flash' %>

<%= will_paginate %>

<div class="table-responsive box-shadow">
    <table class="table table-hover users-table">
        <thead>
            <tr>
                <th class="col-md-3">Name</th>
                <th class="col-md-3">Joined on</th>
                <th class="col-md-5">Learning Progress</th>
                <th class=""></th>
            </tr>
        </thead>
        <tbody>
            <%= render @users %>
        </tbody>
    </table>
</div>

<%= will_paginate %>
